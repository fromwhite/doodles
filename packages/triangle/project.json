{
  "name": "triangle",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/triangle/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/packages/triangle"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nrwl/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "triangle:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "triangle:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "triangle:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nrwl/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "triangle:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "triangle:build:development"
        },
        "production": {
          "buildTarget": "triangle:build:production"
        }
      }
    },
    "test": {
      "executor": "@nrwl/vite:test",
      "outputs": ["coverage/packages/triangle"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/packages/triangle"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/triangle/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
